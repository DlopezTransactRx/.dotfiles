# Changing Windows Focus
ctrl - j : yabai -m window --focus south 
ctrl - k : yabai -m window --focus north 
ctrl - h : yabai -m window --focus west 
ctrl - l : yabai -m window --focus east

# change focus between display (left and right)
shift + ctrl - h: yabai -m display --focus west
shift + ctrl - l: yabai -m display --focus east

# Moving Windows
ctrl + cmd - j : yabai -m window --swap south 
ctrl + cmd - k : yabai -m window --swap north 
ctrl + cmd - h : yabai -m window --swap west 
ctrl + cmd - l : yabai -m window --swap east 

#Moving windows between displays
shift + ctrl + cmd - h : yabai -m window --display west | yabai -m display --focus west
shift + ctrl + cmd - l : yabai -m window --display east | yabai -m display --focus east

# rotate layout clockwise
ctrl - e : yabai -m space --rotate 270

# flip along y-axis
shift + ctrl - x : yabai -m space --mirror y-axis

# flip along x-axis
ctrl - x : yabai -m space --mirror x-axis

# toggle window float
ctrl - t : yabai -m window --toggle float --grig 4:4:1:1:2:2

# maximize a window
ctrl - m : yabai -m window --toggle zoom-fullscreen

# toggle fullscreen for the current window
ctrl - n : yabai -m window --toggle split

# balance out tree of windows (resize to occupy same area) 
shift + ctrl - e : yabai -m space --balance

# # Disable or modify hotkeys when "Ghostty" is active
# ghostty {
#    # No hotkeys will work in this block unless explicitly defined
# }
#
